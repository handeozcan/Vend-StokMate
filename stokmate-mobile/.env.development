# Development varsayılanı (commit'lenir — gizli yok).
# Tek gerçeğe bağlan: barındırılan API (web + dağıtım APK'sı ile aynı veri).
# Yerel .NET API ile çalışmak isterseniz: http://10.0.2.2:5080 (emülatör)
# veya http://<LAN-IP>:5080 (fiziksel cihaz, .env.local içine yazın).
EXPO_PUBLIC_API_BASE_URL=https://stokmate-api-6ec8.onrender.com
