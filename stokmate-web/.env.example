# The .NET API base URL. Web runs on the same machine as the API in development.
# Copy this file to .env.local (.env.local is loaded by both `next dev` and `next build`).
NEXT_PUBLIC_API_BASE_URL=http://localhost:5080
